services:
  
  localstack:
    image: localstack/localstack:3.0.2
    container_name: localstack
    profiles: [ 'infra', 'services' ]
    ports:
      - '4566:4566'
      - '4510-4559:4510-4559'
    env_file:
      - './compose/aws.env'
    environment:
      DEBUG: ${DEBUG:-1}
      LS_LOG: WARN
      SERVICES: s3,sqs,sns,cloudwatch,sts
      LOCALSTACK_HOST: 127.0.0.1
    volumes:
      - localstack-data:/var/lib/localstack
      - ./compose/start-localstack.sh:/etc/localstack/init/ready.d/start-localstack.sh
    healthcheck:
      test: [ 'CMD', 'curl', 'localhost:4566' ]
      interval: 5s
      start_period: 5s
      retries: 3
    networks:
      - trade-imports-tenant
  
  trade-imports-stub:
    container_name: trade-imports-stub
    profiles: [ 'services' ]
    build:
      context: .
      target: development
    depends_on:
      localstack:
        condition: service_healthy
    env_file:
      - './compose/aws.env'
    ports:
      - '8087:8087'
      - '5007:5005' # Java debug port
    environment:
      # Server
      PORT: 8087

      # Service Identity
      SERVICE_VERSION: 0.0.0-local
      ENVIRONMENT: local

      # AWS (via LocalStack)
      LOCALSTACK_ENDPOINT: http://localstack:4566
      AWS_REGION: eu-west-2

      # Logging
      LOG_LEVEL: DEBUG

      # Metrics (disabled locally)
      METRICS_ENABLED: true

      # Spring profile (local = exposes debugging actuator endpoints)
      SPRING_PROFILES_ACTIVE: local

      # Java Debug (optional - uncomment to enable remote debugging)
      # JAVA_TOOL_OPTIONS: -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005
    networks:
      - trade-imports-tenant
    # Note: No volume mounts for Java (requires rebuild for code changes)
    # For faster development, use 'mvn spring-boot:run' directly on host

################################################################################


volumes:
  localstack-data:

networks:
  trade-imports-tenant:
    name: trade-imports-tenant
